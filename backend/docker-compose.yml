version: "3"
services:
  weihua:
    image: weihua
    volumes:
      - ./pb_data:/app/pb_data
    restart: always
    network_mode: host
  yolo:
    image: yolo
    ports:
      - "5000:5000"
    restart: always
